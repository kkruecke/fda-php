1. Understand remaining SPL iterator classes, like LimitIterator and FilterIterato, and how they are to be used in the functional programming paragdigm.

2. Remove setUp() method, putting it in Ctors.

3. Model code more after C++ version and copy_if() than prior php version. Make code clear and methods shorter. Add comments to give basic explanation.

4. Change Registry to read an xml file using SimpleXML

5. Replace any array_search()'es with sort arrays that do binary_search().

6. Incorporate the update_medwatch logic, now in a separate .php file, into main.php.
